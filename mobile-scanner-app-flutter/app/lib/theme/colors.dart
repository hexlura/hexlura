import 'package:flutter/material.dart';

/// Matches mobile-scanner-app/design/shared.css exactly — keep in sync.
class AppColors {
  AppColors._();

  static const bg = Color(0xFF0A0A0F);
  static const surface = Color(0xFF13131A);
  static const card = Color(0xFF1A1A24);
  static const border = Color(0xFF2A2A3A);
  static const text = Color(0xFFF0F0F8);
  static const muted = Color(0xFF8888AA);
  static const accent = Color(0xFFE63950);
  static const accentDim = Color(0xFF7A1A26);
  static const success = Color(0xFF00E5A0);
  static const warning = Color(0xFFF5A623);
  static const footnote = Color(0xFF555570);
}
